// 笔记：视频里面教学是需要写一个vue.config.js的配置文件，
然后再配置别名，经过测试，发现可以直接@/路径这样是可以的（这里的@是指./src）应该是新版本控制好的

在子组件里面定义了props之后，比如说有一个数组变量a，定义完数组变量之后（别忘记default（）是一个函数，return一个[]），在父组件里面,比如要把c的值传入b用  :b="c"，这样子组件就获得了父组件b的值。作展示的时候在子组件里直接使用就ok了

git add .
git commit -m ""
git push